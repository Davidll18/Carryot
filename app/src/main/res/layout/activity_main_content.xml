<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Toolbar
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#FFFFFF">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageButton
                android:id="@+id/buttonMenu"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@drawable/menu"
                android:background="@color/green"
                android:padding="12dp"
                android:contentDescription="Abrir menú"
                android:layout_gravity="start|center_vertical" />

            <ImageView
                android:layout_width="50dp"
                android:layout_height="50dp"
                android:src="@drawable/logo"
                android:layout_marginLeft="160dp" />

        </FrameLayout>
    </Toolbar>

    <!-- Línea delgada de color verde -->
    <View
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:background="#34FF4140" />

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <Button
                android:id="@+id/btnGestionUsuarios"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Gestion de usuarios"
                android:backgroundTint="@color/green"
                android:textColor="#FFFFFF"
                android:layout_marginBottom="16dp"
                android:onClick="btnGestionUsuarios" />

            <Button
                android:id="@+id/btnRegistrarAdmin"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Registrar Admin de restaurant"
                android:backgroundTint="@color/green"
                android:textColor="#FFFFFF"
                android:layout_marginBottom="16dp"
                android:onClick="btnRegistrarAdmin" />

            <Button
                android:id="@+id/btnReporteVentas"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Reporte de ventas"
                android:backgroundTint="@color/green"
                android:textColor="#FFFFFF"
                android:layout_marginBottom="16dp"
                android:onClick="btnReporteVentas" />

            <Button
                android:id="@+id/btnRestaurants"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Restaurants"
                android:backgroundTint="@color/green"
                android:textColor="#FFFFFF"
                android:layout_marginBottom="16dp"
                android:onClick="btnRestaurants" />

            <Button
                android:id="@+id/btnLogs"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Logs"
                android:backgroundTint="@color/green"
                android:textColor="#FFFFFF" />

        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</LinearLayout>
